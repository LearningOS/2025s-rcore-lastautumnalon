# 实验报告

## lab2

- 实现trace、get_time 专门写了rw_byte用于判断rw是否有效

- 实现mmap 调用了分配函数

- 实现unmap 其中写了一个部分解除分配的功能

## homework2

1.页表项有10位reversed，44位物理页号，和低8位的标志位。
V标志页表项的合法性
R W X 分别表示是否可读、可写、可执行
U 表示该页面是否在U特权级允许被访问
G不理会
A为这一位清零后是否被访问过
D为脏位记录是否修改

2.
 - PageFault 虚拟地址在页表中不存在 Segementation Fault 访问未分配内存区域、写一个只读页面、栈溢出但系统未动态扩展栈 （GPT） Copy on Write 写保护异常 （GPT）
 - 描述过了，sp通过与sccratch交换，获得了trap上下文地址。然后在trap上下文中保存寄存器，最后在trapcontext中加载出handler地址进行jr
 - 节省时间和空间
 - 要用2560个页表项存储，一个页表项占1个字节，那么就是2560byte
 - 参考线段树的lazytag（） 缺页的时候加载进来再更新吧。
 - V 置零

3.
 - 设置satp到新的用户页表地址 （GPT + os代码） 而内核页表是共享的。
 - U 位 控制
 - 节省空间
 - 权限级切换的时候切换页表 以及进程调度。 我会选择在进程调度时更换。

在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

《GPT、学长》

此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

《rust、rcore指导书》

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。

